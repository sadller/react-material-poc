import { makeStyles } from "@material-ui/core";

const useStyles = makeStyles({
    home: {
        
    }
})

const Home = props => {
    const classes = useStyles();
    
    return (
        <h2>
            Home
        </h2>
    )
}

export default Home;